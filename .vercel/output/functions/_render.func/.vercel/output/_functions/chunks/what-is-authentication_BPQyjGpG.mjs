import { V as createComponent, a0 as maybeRenderHead, ae as unescapeHTML, aa as renderTemplate } from './astro/server_BnJdSZNs.mjs';
import 'kleur/colors';
import 'clsx';

const html = "<p>Authentication is the process of proving you are who you claim to be. It’s the front door of every secure system. Everything else in security — authorization, encryption, monitoring — depends on authentication working correctly.</p>\n<h2 id=\"authentication-factors\">Authentication factors</h2>\n<p>There are three categories:</p>\n<p><strong>Something you know</strong> — Passwords, PINs, security questions. The most common factor. Also the weakest, because knowledge can be stolen, guessed, or tricked out of you.</p>\n<p><strong>Something you have</strong> — A phone receiving SMS codes, an authenticator app generating time-based codes, a hardware security key, a smart card. Stronger because an attacker needs physical access to your device.</p>\n<p><strong>Something you are</strong> — Biometrics: fingerprints, facial recognition, iris scans, voice patterns. Hard to fake (but not impossible — see below) and convenient for the user.</p>\n<p>A system that requires only one factor is <strong>single-factor authentication</strong>. One password. That’s it. This is the minimum and it’s increasingly considered insufficient.</p>\n<p>A system that requires two or more factors is <strong>multi-factor authentication (MFA)</strong> — we covered this in depth in a separate article. Refer to that for the full picture.</p>\n<h2 id=\"how-passwords-actually-work-server-side\">How passwords actually work (server-side)</h2>\n<p>When you create an account, the server doesn’t store your password. It stores a <strong>hash</strong> of your password — a one-way mathematical transformation.</p>\n<pre class=\"astro-code github-dark\" style=\"background-color:#24292e;color:#e1e4e8; overflow-x: auto;\" tabindex=\"0\" data-language=\"plaintext\"><code><span class=\"line\"><span>Your password: \"correct horse battery staple\"</span></span>\n<span class=\"line\"><span>Hash produced: \"ef92b778bafe771e89245b89ecbc08a44a4e166c06659911881f383d4473e94f\"</span></span>\n<span class=\"line\"><span></span></span></code></pre>\n<p>When you log in, the server hashes the password you entered and compares it to the stored hash. If they match, you’re in. If the database is breached, the attacker gets hashes, not passwords.</p>\n<p>But here’s the catch: <strong>not all hashing is equal.</strong></p>\n<ul>\n<li><strong>Old hashing</strong> (MD5, SHA-1) is fast and reversible with enough computing power. These are broken.</li>\n<li><strong>Proper hashing</strong> (bcrypt, scrypt, Argon2) is deliberately slow and includes a random “salt” so the same password produces different hashes each time. This makes brute-force attacks impractical.</li>\n</ul>\n<p>When a service tells you “your password was leaked in plaintext” — that means they were storing passwords without hashing (or with reversible encryption). This is unacceptable and indicates fundamental security incompetence.</p>\n<h2 id=\"password-attacks\">Password attacks</h2>\n<p><strong>Brute force</strong> — Try every possible combination. Modern GPUs can try billions of hashes per second against weak hashing algorithms.</p>\n<p><strong>Dictionary attack</strong> — Try common passwords and variations. Quick and effective against human-chosen passwords.</p>\n<p><strong>Credential stuffing</strong> — Use username/password pairs from one breach to try logging into other services. Exploits password reuse. This is automated and incredibly common.</p>\n<p><strong>Rainbow tables</strong> — Pre-computed tables of password-to-hash mappings. Mitigated by salting.</p>\n<p><strong>Phishing</strong> — Trick the user into entering their password on a fake site. Bypasses all cryptographic protections.</p>\n<h2 id=\"modern-approaches\">Modern approaches</h2>\n<p><strong>Passwordless authentication</strong> — Eliminate passwords entirely. Methods include:</p>\n<ul>\n<li><strong>Magic links</strong> — Email a one-time login link</li>\n<li><strong>WebAuthn / Passkeys</strong> — Cryptographic credentials stored on your device, authenticated with biometrics or PIN. Supported by all major platforms. This is the future of authentication.</li>\n<li><strong>FIDO2 / hardware keys</strong> — Physical devices that prove identity cryptographically</li>\n</ul>\n<p><strong>Single Sign-On (SSO)</strong> — Log in once with an identity provider (Google, Microsoft, Okta) and access multiple services without separate passwords. Convenient but creates a single point of failure — if your SSO account is compromised, everything connected to it is at risk.</p>\n<p><strong>OAuth / OpenID Connect</strong> — Protocols that let you authorize one application to access your data from another without sharing your password. When you click “Sign in with Google,” you’re using OAuth.</p>\n<h2 id=\"the-bottom-line\">The bottom line</h2>\n<p>Authentication is trust establishment. Every security decision the system makes after you log in depends on the assumption that you are who you claim to be. If authentication fails, everything built on top of it fails too.</p>\n<p>The industry is moving away from passwords toward more secure and usable alternatives. Passkeys and hardware keys are the direction things are heading. But for now, unique strong passwords plus MFA remain the best practical approach for most people.</p>";

				const frontmatter = {"title":"What is Authentication?","topic":"Identity & Access","order":1,"readTime":"5 min read"};
				const file = "/Users/karthikeyan/hackzoo-owl/cyberplain/src/content/articles/what-is-authentication.md";
				const url = undefined;
				function rawContent() {
					return "\nAuthentication is the process of proving you are who you claim to be. It's the front door of every secure system. Everything else in security — authorization, encryption, monitoring — depends on authentication working correctly.\n\n## Authentication factors\n\nThere are three categories:\n\n**Something you know** — Passwords, PINs, security questions. The most common factor. Also the weakest, because knowledge can be stolen, guessed, or tricked out of you.\n\n**Something you have** — A phone receiving SMS codes, an authenticator app generating time-based codes, a hardware security key, a smart card. Stronger because an attacker needs physical access to your device.\n\n**Something you are** — Biometrics: fingerprints, facial recognition, iris scans, voice patterns. Hard to fake (but not impossible — see below) and convenient for the user.\n\nA system that requires only one factor is **single-factor authentication**. One password. That's it. This is the minimum and it's increasingly considered insufficient.\n\nA system that requires two or more factors is **multi-factor authentication (MFA)** — we covered this in depth in a separate article. Refer to that for the full picture.\n\n## How passwords actually work (server-side)\n\nWhen you create an account, the server doesn't store your password. It stores a **hash** of your password — a one-way mathematical transformation.\n\n```\nYour password: \"correct horse battery staple\"\nHash produced: \"ef92b778bafe771e89245b89ecbc08a44a4e166c06659911881f383d4473e94f\"\n```\n\nWhen you log in, the server hashes the password you entered and compares it to the stored hash. If they match, you're in. If the database is breached, the attacker gets hashes, not passwords.\n\nBut here's the catch: **not all hashing is equal.**\n\n- **Old hashing** (MD5, SHA-1) is fast and reversible with enough computing power. These are broken.\n- **Proper hashing** (bcrypt, scrypt, Argon2) is deliberately slow and includes a random \"salt\" so the same password produces different hashes each time. This makes brute-force attacks impractical.\n\nWhen a service tells you \"your password was leaked in plaintext\" — that means they were storing passwords without hashing (or with reversible encryption). This is unacceptable and indicates fundamental security incompetence.\n\n## Password attacks\n\n**Brute force** — Try every possible combination. Modern GPUs can try billions of hashes per second against weak hashing algorithms.\n\n**Dictionary attack** — Try common passwords and variations. Quick and effective against human-chosen passwords.\n\n**Credential stuffing** — Use username/password pairs from one breach to try logging into other services. Exploits password reuse. This is automated and incredibly common.\n\n**Rainbow tables** — Pre-computed tables of password-to-hash mappings. Mitigated by salting.\n\n**Phishing** — Trick the user into entering their password on a fake site. Bypasses all cryptographic protections.\n\n## Modern approaches\n\n**Passwordless authentication** — Eliminate passwords entirely. Methods include:\n- **Magic links** — Email a one-time login link\n- **WebAuthn / Passkeys** — Cryptographic credentials stored on your device, authenticated with biometrics or PIN. Supported by all major platforms. This is the future of authentication.\n- **FIDO2 / hardware keys** — Physical devices that prove identity cryptographically\n\n**Single Sign-On (SSO)** — Log in once with an identity provider (Google, Microsoft, Okta) and access multiple services without separate passwords. Convenient but creates a single point of failure — if your SSO account is compromised, everything connected to it is at risk.\n\n**OAuth / OpenID Connect** — Protocols that let you authorize one application to access your data from another without sharing your password. When you click \"Sign in with Google,\" you're using OAuth.\n\n## The bottom line\n\nAuthentication is trust establishment. Every security decision the system makes after you log in depends on the assumption that you are who you claim to be. If authentication fails, everything built on top of it fails too.\n\nThe industry is moving away from passwords toward more secure and usable alternatives. Passkeys and hardware keys are the direction things are heading. But for now, unique strong passwords plus MFA remain the best practical approach for most people.\n";
				}
				function compiledContent() {
					return html;
				}
				function getHeadings() {
					return [{"depth":2,"slug":"authentication-factors","text":"Authentication factors"},{"depth":2,"slug":"how-passwords-actually-work-server-side","text":"How passwords actually work (server-side)"},{"depth":2,"slug":"password-attacks","text":"Password attacks"},{"depth":2,"slug":"modern-approaches","text":"Modern approaches"},{"depth":2,"slug":"the-bottom-line","text":"The bottom line"}];
				}

				const Content = createComponent((result, _props, slots) => {
					const { layout, ...content } = frontmatter;
					content.file = file;
					content.url = url;

					return renderTemplate`${maybeRenderHead()}${unescapeHTML(html)}`;
				});

export { Content, compiledContent, Content as default, file, frontmatter, getHeadings, rawContent, url };
